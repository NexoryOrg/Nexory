name: Discord Notifications

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, closed, reopened, edited]
  issues:
    types: [opened, edited, closed, reopened]
  issue_comment:
    types: [created, edited, deleted]
  pull_request_review_comment:
    types: [created, edited, deleted]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:

      # =========================
      # PUSH
      # =========================

      - name: Build push embed
        id: build_push
        if: github.event_name == 'push'
        shell: bash
        run: |
          python - <<'PY'
          import json, os

          ev = json.load(open(os.environ['GITHUB_EVENT_PATH']))
          head = ev.get('head_commit', {})
          commits = ev.get('commits', [])

          added, removed, modified = set(), set(), set()
          additions = deletions = 0

          for c in commits:
              added.update(c.get("added", []))
              removed.update(c.get("removed", []))
              modified.update(c.get("modified", []))
              additions += c.get("stats", {}).get("additions", 0)
              deletions += c.get("stats", {}).get("deletions", 0)

          files = list(added | removed | modified)
          file_list = "\n".join(files[:10])
          if len(files) > 10:
              file_list += f"\n+ {len(files)-10} more files"

          data = [{
            "title": f"Push to {os.environ.get('GITHUB_REF_NAME','')}",
            "description": f"Comment (`{head.get('id','')}`):\n{head.get('message','')}",
            "color": 3447003,
            "author": {
              "name": os.environ.get('GITHUB_ACTOR',''),
              "url": f"https://github.com/{os.environ.get('GITHUB_ACTOR','')}",
              "icon_url": f"https://github.com/{os.environ.get('GITHUB_ACTOR','')}.png"
            },
            "fields": [
              {"name": "Changed Files", "value": file_list or "None", "inline": False},
              {"name": "Additions", "value": str(additions), "inline": True},
              {"name": "Deletions", "value": str(deletions), "inline": True}
            ],
            "timestamp": head.get('timestamp',''),
            "footer": {"text": os.environ.get('GITHUB_REPOSITORY','')}
          }]

          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write('embeds<<EOF\n')
              f.write(json.dumps(data))
              f.write('\nEOF\n')
          PY

      - name: Notify push
        if: github.event_name == 'push'
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_EMBEDS: ${{ steps.build_push.outputs.embeds }}

      # =========================
      # PULL REQUEST
      # =========================

      - name: Build pull request embed
        id: build_pr
        if: github.event_name == 'pull_request'
        shell: bash
        run: |
          python - <<'PY'
          import json, os

          ev = json.load(open(os.environ['GITHUB_EVENT_PATH']))
          pr = ev.get('pull_request', {})
          user = pr.get('user', {})
          action = ev.get('action')

          if action in ["opened", "reopened"]:
              color = 3066993
          elif action == "closed":
              color = 15158332
          else:
              color = 9807270

          data = [{
            "title": f"Pull Request #{pr.get('number')}",
            "description": pr.get('title',''),
            "color": color,
            "author": {
              "name": user.get('login',''),
              "url": f"https://github.com/{user.get('login','')}",
              "icon_url": f"https://github.com/{user.get('login','')}.png"
            },
            "fields": [
              {"name": "Action", "value": action, "inline": True},
              {"name": "Branch", "value": pr.get("head", {}).get("ref",""), "inline": True}
            ],
            "url": pr.get('html_url',''),
            "footer": {"text": os.environ.get('GITHUB_REPOSITORY','')}
          }]

          with open(os.environ['GITHUB_OUTPUT'],'a') as f:
              f.write('embeds<<EOF\n')
              f.write(json.dumps(data))
              f.write('\nEOF\n')
          PY

      - name: Notify pull request
        if: github.event_name == 'pull_request'
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_EMBEDS: ${{ steps.build_pr.outputs.embeds }}

      # =========================
      # ISSUES
      # =========================

      - name: Build issue embed
        id: build_issue
        if: github.event_name == 'issues'
        shell: bash
        run: |
          python - <<'PY'
          import json, os

          ev = json.load(open(os.environ['GITHUB_EVENT_PATH']))
          issue = ev.get('issue', {})
          user = issue.get('user', {})
          action = ev.get('action')

          if action in ["opened", "reopened"]:
              color = 3066993
          elif action == "closed":
              color = 15158332
          else:
              color = 9807270

          data = [{
            "title": f"Issue #{issue.get('number')}",
            "description": issue.get('title',''),
            "color": color,
            "author": {
              "name": user.get('login',''),
              "url": f"https://github.com/{user.get('login','')}",
              "icon_url": f"https://github.com/{user.get('login','')}.png"
            },
            "fields": [
              {"name": "Action", "value": action, "inline": True}
            ],
            "url": issue.get('html_url',''),
            "footer": {"text": os.environ.get('GITHUB_REPOSITORY','')}
          }]

          with open(os.environ['GITHUB_OUTPUT'],'a') as f:
              f.write('embeds<<EOF\n')
              f.write(json.dumps(data))
              f.write('\nEOF\n')
          PY

      - name: Notify issue
        if: github.event_name == 'issues'
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_EMBEDS: ${{ steps.build_issue.outputs.embeds }}

      # =========================
      # ISSUE COMMENT
      # =========================

      - name: Build issue comment embed
        id: build_issue_comment
        if: github.event_name == 'issue_comment'
        shell: bash
        run: |
          python - <<'PY'
          import json, os

          ev = json.load(open(os.environ['GITHUB_EVENT_PATH']))
          comment = ev.get('comment', {})
          issue = ev.get('issue', {})
          user = comment.get('user', {})

          data = [{
            "title": f"Comment on Issue #{issue.get('number','')}",
            "description": comment.get('body',''),
            "color": 9807270,
            "author": {
              "name": user.get('login',''),
              "url": f"https://github.com/{user.get('login','')}",
              "icon_url": f"https://github.com/{user.get('login','')}.png"
            },
            "url": comment.get('html_url',''),
            "footer": {"text": os.environ.get('GITHUB_REPOSITORY','')}
          }]

          with open(os.environ['GITHUB_OUTPUT'],'a') as f:
              f.write('embeds<<EOF\n')
              f.write(json.dumps(data))
              f.write('\nEOF\n')
          PY

      - name: Notify issue comment
        if: github.event_name == 'issue_comment'
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_EMBEDS: ${{ steps.build_issue_comment.outputs.embeds }}

      # =========================
      # PR REVIEW COMMENT
      # =========================

      - name: Notify PR review comment
        if: github.event_name == 'pull_request_review_comment'
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_EMBEDS: |
            [
              {
                "title": "PR Review Comment",
                "description": "${{ github.event.comment.body }}",
                "color": 9807270,
                "author": {
                  "name": "${{ github.event.comment.user.login }}",
                  "url": "https://github.com/${{ github.event.comment.user.login }}",
                  "icon_url": "https://github.com/${{ github.event.comment.user.login }}.png"
                },
                "url": "${{ github.event.comment.html_url }}",
                "footer": { "text": "${{ github.repository }}" }
              }
            ]